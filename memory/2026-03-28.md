- New rule from user: when sharing info gathered from websites, include the source URL(s) in the reply by default.
- New rule from user: for weather or similar factual lookups, if the first retrieval fails, explicitly report the failure and retry once; if retry also fails, say so clearly instead of silently omitting that part.
